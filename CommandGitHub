git config --global user.email "mukesh_kumar1026@hotmail.com"
git config --global user.name "Mukesh Kumar"


Learn Git Part 1=============

git init
git add [filename] || -A
git status
git commit -m "msg"
git remote add origin repoName
git push origin master
git clone repoName
git pull origin master
